import { MileToKmPipePipe } from './mile-to-km-pipe.pipe';

describe('MileToKmPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MileToKmPipePipe();
    expect(pipe).toBeTruthy();
  });
});
